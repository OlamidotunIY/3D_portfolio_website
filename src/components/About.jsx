import React from "react";
// React tilt
import { Tilt } from "react-tilt";
// Framer Motion
import { motion } from "framer-motion";
// Import styles
import { styles } from "../styles";
import { services } from "../constant/index";
import { fadeIn, textVariant } from "../utils/motion";
import { SectionWrapper } from "../hoc";

const ServiceCard = ({ index, title, icon }) => {
  return (
    <Tilt className="xs:w-[250px] w-full">
      <motion.div
        variants={fadeIn("right", "spring", 0.5 * index, 0.75)}
        className="w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card"
      >
        <div
          options={{
            max: 45,
            scale: 1,
            speed: 450,
          }}
          className="bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly flex-col items-center"
        >
          <img src={icon} alt={title} className="w-16 h-16 object-contain" />
          <h3 className="text-white text-[20px] font-bold text-center">
            {title}
          </h3>
        </div>
      </motion.div>
    </Tilt>
  );
};

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview</h2>
      </motion.div>
      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className="mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]"
      >
        I am a passionate web and mobile full-stack developer with 4 years of
        experience building robust and scalable applications. Proficient in
        frameworks like NestJS, Next.js, Node.js, Express, and React, I
        specialize in creating dynamic, user-friendly solutions for diverse
        needs. Whether developing responsive web applications or
        high-performance mobile platforms, I bring a deep understanding of
        front-end and back-end technologies to deliver seamless user
        experiences. With a commitment to clean code and best practices, I turn
        complex ideas into efficient, maintainable systems.
      </motion.p>

      <div className="mt-20 flex flex-wrap gap-10">
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
